---
description: Design adversarial tests for a Reader TDD work item
argument-hint: "<WI-id and acceptance criteria>"
---
Load `/skill:reader-tdd`. Design the test matrix for:

$ARGUMENTS

Do not edit product implementation. Include behavior assertions, boundaries, malformed/failure cases, Reader invariants, mock-to-integration counterparts, and the simplest incorrect implementations that could fool weak tests. Identify the focused command to pass to `tdd_red`.
