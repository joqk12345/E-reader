---
description: Plan a Reader V2 change as small TDD work items
argument-hint: "<feature-or-fix>"
---
Load `/skill:reader-tdd`. Plan the following Reader V2 change without editing product code yet:

$ARGUMENTS

Map it to the V2 refactor plan, list affected layers and invariants, then split it into ordered 15–60 minute `WI-*` items. Give each item observable acceptance criteria, test lane, risks, and non-goals. Save substantial plans under `docs/plans/`.
