reader is a Tauri v2 + React + TypeScript desktop app.
Frontend: src/ (React UI, Zustand stores, routes).
Backend: src-tauri/src/ (Rust commands, filesystem integration).
Common validation commands: npm run build, cargo check --manifest-path src-tauri/Cargo.toml.
Prefer root-cause fixes and minimal diffs; note frontend/backend interface impacts when both sides change.
