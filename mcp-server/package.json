{
  "name": "@reader/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server for Reader business tools",
  "bin": {
    "reader-mcp-server": "./dist/cli.js",
    "reader-cli": "./dist/cli.js"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R src/* dist/ && cp dist/cli.mjs dist/cli.js && chmod +x dist/cli.js"
  }
}
